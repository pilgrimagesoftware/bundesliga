# Tasks: app-cargo-metadata

- [ ] 11.1 Update `authors = ["you"]` in `app/src-tauri/Cargo.toml` to the correct author name and email (e.g., `authors = ["Paul Schifferer <paul@schifferers.net>"]`)
- [ ] 11.2 Update `openligadb = "0.0.8"` to `openligadb = "0.0.9"` in `app/src-tauri/Cargo.toml`
- [ ] 11.3 Review the `openligadb` CHANGELOG for any breaking changes between 0.0.8 and 0.0.9
- [ ] 11.4 Run `cargo build` in `app/src-tauri/` to confirm the app compiles with the updated dependency
